# Leave DATABASE_URL blank locally — the backend will use SQLite at backend/data.sqlite.
# On Render, DATABASE_URL is injected automatically from the Postgres database (see render.yaml).
# DATABASE_URL=postgres://user:password@host:5432/dbname

PORT=3001
NODE_ENV=development
