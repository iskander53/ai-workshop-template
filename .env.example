# Leave DATABASE_URL blank locally → backend uses SQLite (./data.sqlite).
# On Render, DATABASE_URL is injected automatically from the Postgres add-on.
# DATABASE_URL=

PORT=3001
NODE_ENV=development
