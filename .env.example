# Leave DATABASE_URL blank locally — backend falls back to a SQLite file.
# On Render, DATABASE_URL is injected automatically from the Postgres database.
# DATABASE_URL=postgres://user:pass@host:5432/dbname

PORT=3001
NODE_ENV=development
